interface:
  display_name: "Socialify"
  short_description: "Repo-specific patterns and workflows for socialify"
  default_prompt: "Use the socialify repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true